extends(src='layout.sgr')
  block(name='content')

		each(loop='page in contentful.textPages')
			if(condition="page.fields.footerLinkName.toLowerCase().indexOf('privacy') >= 0 ")
				script.
					console.log(page.fields.headline)
				// h2.textPage page.fields.headline
				// article.textPage(md) page.fields.body
			else
				script.
					location = "/"

		include(src='_footer.sgr')
